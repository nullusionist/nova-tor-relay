# === Required (please update to your own email) ===
CONTACT=your@email.com

# === Optional: Auto-generated if not set ===
# NICKNAME=NovaRelayCustom

# === Bandwidth Settings ===
BANDWIDTH_LIMIT=100 KB
BANDWIDTH_BURST=200 KB

# === Tor Ports (defaulted in script if not provided) ===
# OR_PORT=9001
# DIR_PORT=9030
# SOCKS_PORT=0

# === Set both to enable control port access ===
# CONTROL_PORT=0.0.0.0:9051
# COOKIE_AUTH=1

# === Optional: Exit Node Settings ===
# EXIT_RELAY=1
# EXIT_POLICY=accept *:*

# === Optional: Accounting Cap ===
# ACCOUNTING_START=day 1 00:00
# ACCOUNTING_MAX=5 GB

# === Optional: Extra Lines Injected Directly into torrc ===
# TOR_EXTRA_LINES=MyFamily $FINGERPRINT1,$FINGERPRINT2\nLog notice stdout
